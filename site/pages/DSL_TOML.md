# dsl-toml

This module defines the canonical TOML workflow schema and will host
parser/serializer logic for text-based workflow definitions.

Canonical schema:

- `SCHEMA.md`

Current canonical routing keys:

- `continue` for fallback routing entries
- `routing` for named branch routing entries
